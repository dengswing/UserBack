1、打包 8h
  1.1 assetBundle的绑定名称由项目组自己决定，依赖关系设定成同一组bundle
  1.2 需要打包的文件放到Resource文件目录下
  1.3 打包脚本编写，分pc、android、ios打包
  1.4 记录一个version.txt的文件，用来管理版本号，采用{version，bundleName}  
2、加载 8h
   2.1 采用WWW.LoadFromCacheOrDownload的形式,版本号读version.txt里面的
   2.2 加载数量限制，同时加载5个
   2.3 加载进度条，提供每个的当前进度和总进度
   2.6 提交一个外部方法，启动加载，加载完成委托回调
   2.7 加载好了，是否需要写入到persistentDataPath ？
3、读取 8h
   3.1 本地采用Resource.Load加载
   3.2 手机采用assetbundle.LoadAssetAsync方式获得
   3.3 提供外部方法，传入路径、类型来获取资源
4、测试及debug  16h 
   4.1 测试本地、Android、Ios版本
   